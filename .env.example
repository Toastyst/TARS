# TARS project workspace — reference env (copy to .env for local scripts)
# Hermes profile secrets live in ~/.hermes/profiles/tars/.env

TARS_WORKSPACE=C:/path/to/TARS
SEARXNG_BASE_URL=http://127.0.0.1:8080
OPEN_METEO_USER_AGENT=TARS/0.1 (contact@example.com)